import styled from 'styled-components';
import { themeConfig } from '../../styles/theme';

export const FooterContainer = styled.footer`
  p {
    text-align: center;
    padding-bottom: ${themeConfig.spacing.large};
  }
`;
